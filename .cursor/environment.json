{
  "name": "Poké Auction",
  "install": "pnpm install --frozen-lockfile\npnpm build:client",
  "terminals": [
    {
      "name": "server",
      "command": "pnpm dev:server",
      "description": "Cloudflare Worker + SQLite Durable Object API via wrangler dev on http://localhost:8787."
    },
    {
      "name": "client",
      "command": "pnpm dev:client",
      "description": "Vite + React client on http://localhost:5173; proxies /api and WebSockets to the Worker on 8787."
    }
  ],
  "ports": [
    { "name": "client", "port": 5173 },
    { "name": "api", "port": 8787 }
  ]
}
